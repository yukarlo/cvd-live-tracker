<resources>
    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <item name="colorPrimary">@color/white50</item>
        <item name="colorPrimaryDark">@color/white300</item>
        <item name="colorAccent">@color/teal200</item>

        <item name="android:textColor">@color/gray800</item>
        <item name="subtitleTextColor">@color/gray500</item>

        <item name="android:windowBackground">@color/white50</item>
        <item name="background">@color/white50</item>

        <item name="colorPrimarySurface">@color/white300</item>
        <item name="colorOnPrimarySurface">@color/gray300</item>

        <item name="cardBackgroundColor">@color/white300</item>

        <item name="colorSurface">@color/white300</item>
        <item name="colorOnSurface">@color/gray300</item>
    </style>

    <style name="Theme.App.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.App.AppBarOverlay" parent="ThemeOverlay.AppCompat.DayNight.ActionBar" />

    <style name="Theme.App.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight" />

</resources>